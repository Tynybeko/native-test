import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        flexDirection: 'row',
    },
    inner: {
        width: '100%',
        paddingTop: 10,

    },
    text: {
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        textAlign: 'center',
    }
})